# Uncomment the next line to define a global platform for your project
target 'flash (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  platform :ios, '14.0'
  use_frameworks!
  
  # Pods for flash (iOS)
  pod 'RealmSwift', '~>10'
end

target 'flash (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  platform :osx, '12.0'
  use_frameworks!
  
  # Pods for flash (macOS)
  pod 'RealmSwift', '~>10'
end

target 'Share' do
  platform :ios, '14.0'
  use_frameworks!
  
  # Pods for flash (macOS)
  pod 'RealmSwift', '~>10'
end

target 'Tests iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  platform :ios, '14.0'
  use_frameworks!
  
  # Pods for flash (iOS)
  pod 'RealmSwift', '~>10'
  pod 'ViewInspector'
end
